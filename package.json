{
  "name": "es6-babel-mocha-expect",
  "version": "1.0.0",
  "description": "Small starter to play with ES6 and make some tests",
  "main": "src/index.js",
  "scripts": {
    "test:watch": "watch-run -i -p 'src/**/*' npm run test",
    "test": "npm run compile && mocha --colors lib/*.spec.js lib/**/*.spec.js",
    "compile": "rimraf ./lib/**/* && babel src -d lib"
  },
  "author": "Maxime Robert <maxime.robert1992@gmail.com>",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "6.9.0",
    "babel-preset-es2015": "6.9.0",
    "expect": "1.20.1",
    "rimraf": "2.5.2",
    "watch-run": "1.2.4"
  }
}
